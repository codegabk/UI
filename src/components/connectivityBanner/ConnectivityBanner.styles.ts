import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bannerText: {
    fontSize: 16,
    fontWeight: '500',
  },
  banner: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
